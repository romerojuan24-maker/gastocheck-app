import { Redirect } from 'expo-router'

// CobraCheck entry point — the full route screen lives in mi-ruta.tsx
export default function CobraCheckScreen() {
  return <Redirect href="/cobracheck/mi-ruta" />
}
